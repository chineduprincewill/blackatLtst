import React from 'react';
import { NotificationList } from '@components/NotificationNew';
import NotificationsNew from '@components/NotificationNew';

export default function NotificationsMobile() {
    return (
        <div>
            <NotificationList />
        </div>
    );
}
